{
  "name": "lcmc-vault-backups",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "version": "0.6.2",
  "scripts": {
    "clean": "rm -rf node_modules",
    "test": "bun test",
    "typecheck": "tsc --noEmit --skipLibCheck --esModuleInterop --target esnext --moduleResolution bundler --module esnext --strict ./src/**/*.ts",
    "debug": "bun run ./src/index.ts",
    "compile": "bun run ./scripts/compile",
    "start": "./build/bin/lcmc-vault-backups"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@cleverjs/cli": "^2.4.0",
    "dotenv": "^17.4.2",
    "flexbuf": "^1.5.1",
    "low-level": "^1.2.5"
  }
}